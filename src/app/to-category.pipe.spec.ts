import { ToCategoryPipe } from './to-category.pipe';

describe('ToCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ToCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
